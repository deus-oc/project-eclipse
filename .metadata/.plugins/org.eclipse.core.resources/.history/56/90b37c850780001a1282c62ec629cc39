package Program;
import javax.swing.*;

public class allReportFrame {
	private JFrame report = new JFrame("Full Report");
	public allReportFrame() {
		report.setSize(300,300);
		report.setVisible(true);
	}
}