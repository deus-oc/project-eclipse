package Program;
import javax.swing.*;
public class reportFrame {
	private JFrame report = new JFrame("Report Card");
	public reportFrame() {
		report.setSize(300,300);
		report.setVisible(true);
	}
}